export interface Song{
    name: string;
    id: number;
    picUrl: string
}

export interface SongDetail{
    name: string;
    // artist: string;
    id: number;
    duration: string;
    // albumid: string;
    url: string;
}